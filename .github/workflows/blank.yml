name: Update Profile Badge section from Credly

on:
  workflow_dispatch:
    inputs:
      target:
        description: "update target"
        required: false
        default: 'badges' 
        
jobs:
  update-badge:
    name: Update README Badge Section 
    runs-on: ubuntu-latest
    permissions:
      content: write
    steps: 
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:
          CREDLY_USER: taejunkim
